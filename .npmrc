# SM Payroll System - NPM Configuration
# This file ensures npm install NEVER fails due to peer dependency conflicts.
# React-Leaflet@5 requires React 19, but project uses React 18 — this flag bypasses that check.
legacy-peer-deps=true
