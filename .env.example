# Frontend Environment Variables
# Copy this to .env.local for development, or set in Vercel Dashboard for production

# Backend API URL (Render deployed backend)
# Dev (local): leave empty — Vite proxy handles /api → localhost:3000
# Production (Vercel): set this to your Render URL
VITE_API_URL=
